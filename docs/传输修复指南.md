# 传输修复指南

主对话合并时对照此表，一行一行处理。

## 修复清单

| # | 问题 | 改哪个文件 | 改什么 | 为什么 | 归属 | 自测 | 状态 |
|---|------|-----------|--------|--------|------|------|------|
| 1 | /video_feed 连接泄漏 | `app_fixed.py` | 限制同时 1 个活跃连接，新连接来踢旧 | 浏览器重连耗尽 6 连接池 | B | ✓ | ✅ 已合并 |
| 2 | generate_frames 无退出信号 | `detector_fixed.py` | 加 `threading.Event`，客户端断开后退出循环 | 旧线程不退出，僵尸积累 | B | ✓ | ✅ 已合并 |
| 3 | callback() 中 delay(30) 阻塞 | `ESP8266_MQTT_fixed.ino` | 去掉 delay，改用 `millis()` 非阻塞计时 | 每次消息阻塞 60ms，可能断 keepalive | B | ✓ | ✅ 已合并 |
| 4 | 重连只试有限次 | `ESP8266_MQTT_fixed.ino` | 无限重连 + 指数退避 | WiFi/MQTT 断线后放弃 | B | ✓ | ✅ 已合并 |
| 5 | 钉钉报警刷屏 | `notify_fixed.py` | 发送冷却：红30s/黄60s/升级穿透/解除仅全恢复时发 | 三通道独立检测导致消息频繁 | B | ✓ py_compile | 待审查 |
| 6 | 多 STM32 MQTT 接入 | `communication.py` | 通配符订阅 +/status|flame + devices 字典 + broadcast 遍历在线设备 | 单 topic 只能支持一块 STM32 | B | ✓ py_compile | ✅ 已合并 |
| 6a | ESP8266 多设备 topic | `ESP8266_MQTT_multi.ino` | DEVICE_ID 宏 + topic 拼 bishe/99257/{ID}/... | topic 写死无法区分设备 | B | — | ✅ 已合并 |
| 7 | MJPEG 画面冻结 | `detector.py` | _mjpeg_streams 持久连接缓存 + _read_mjpeg 复用 TCP | 每帧新建 HTTP 连接导致 ESP32-CAM 连接堆积 | B | ✓ py_compile | ✅ 已合并 |
| 7a | MJPEG 读帧失败（#7 引入的 bug） | `detector.py` | ①删探针连接 ②per-thread 缓存 ③r.raw.read() 替代 iter_content() | 探针占坑致连接被拒、生成器 GC 关闭连接 | B | ✓ py_compile | ✅ 已合并（仍有 bug，见 #7b） |
| 7b | MJPEG 画面冻结（#7a buf 丢数据） | `detector.py` | 持久化 iter_content() 生成器 + buf 到 _mjpeg_connections 跨调用复用 | buf=b'' 每调用重建丢失残余流数据，r.raw.read() 阻塞超时 | B | ✓ py_compile | ✅ 已合并 |

自测：子对话产出后填写（✓=通过, ✗=有问题, —=未开始）
状态流转：`待开始` → `待审查` → `已合并` / `退回修改`

## 合并步骤

```bash
# 1. 查看变更
diff campus_monitor/app.py campus_monitor/app_fixed.py

# 2. 确认无误后合并
cp campus_monitor/app_fixed.py campus_monitor/app.py

# 3. 清理
rm campus_monitor/app_fixed.py

# 4. 更新本表状态
```

## 依赖关系

```
#1 + #2 = 同一机制，合并时一起 diff、一起 commit
#3 + #4 = 同一文件，一起修
#6 + #6a = 跨层依赖：communication 通配符 + ESP8266 per-device topic
```

- #1+#2 拆开谁也跑不起来，审查必须一起看
- #3+#4 同文件可一次修完
- #6+#6a 跨层依赖：communication.py 订阅通配符 topic，ESP8266 固件发布到 per-device topic，需一起合并


## 协作进度

| 日期 | 内容 | 状态 |
|------|------|------|
| 2026-05-15 | 对话B产出 #1+#2+#3+#4 全部 _fixed 文件 | ✅ 完成 |
| 2026-05-16 | 对话B产出 #5 notify_fixed.py：发送冷却限流，解决三通道报警刷屏 | ✅ 完成 |
| 2026-05-17 | 重命名 tcp_server.py → communication.py（MQTT 主力通信，消除命名误导） | ✅ 完成 |
| 2026-05-17 | 对话B产出 #6 communication_fixed.py + #6a ESP8266_MQTT_multi.ino | ✅ 已合并 |
| 2026-05-17 | 对话B产出 #7 MJPEG 持久连接 — 分支 `feature/mjpeg-persistent-connection` | ✅ 已合并 |
| 2026-05-17 | #7a MJPEG 读帧修复 + #7b buf 丢数据修复 | ✅ 已合并 |

## 产出清单

| 文件 | 覆盖修复 | 自测 |
|------|---------|------|
| `campus_monitor/app_fixed.py` | #1 连接去重 | Python 编译通过, diff 变更范围符合预期 |
| `campus_monitor/detector_fixed.py` | #2 stop_event | Python 编译通过 |
| `Arduino_camera/ESP8266_MQTT/ESP8266_MQTT_fixed.ino` | #3 去delay + #4 无限重连 | Arduino 编译未验证 |
| `campus_monitor/notify_fixed.py` | #5 钉钉发送冷却限流 | py_compile 通过 |
| `campus_monitor/communication_fixed.py` | #6 多设备 MQTT 通配符 | py_compile 通过 |
| `Arduino_camera/ESP8266_MQTT_multi/ESP8266_MQTT_multi.ino` | #6a ESP8266 DEVICE_ID | Arduino 编译未验证 |
| `campus_monitor/detector.py`（分支 `feature/mjpeg-persistent-connection`） | #7 MJPEG 持久连接 | py_compile 通过 |
| `campus_monitor/detector.py`（分支 `feature/mjpeg-read-fix`） | #7a MJPEG 读帧修复 | py_compile 通过 |
| `campus_monitor/detector.py`（分支 `feature/mjpeg-gen-fix`） | #7b 持久化 gen+buf | py_compile 通过 |

### 主对话合并注意事项

1. **#1+#2 必须一起合并**：app_fixed.py 传 stop_event，detector_fixed.py 接收 stop_event，拆开不工作
2. **#3+#4 独立合并**：ESP8266 固件改动，不影响 Python 端
3. **#5 独立合并**：notify_fixed.py 新增文件，合并后删除旧 notify.py。API 签名不变。冷却默认值 `_COOLDOWN_RED=30` / `_COOLDOWN_YELLOW=60` / `_COOLDOWN_CLEAR=30`（秒）可调。
4. **#6 向后兼容**：`communication.py` 保留 `flame_active`/`stm32_connected` 聚合属性，新增 STM32 零代码改动。合并时 app.py 删除 TCP 线程启动。
5. **#6+#6a 联动**：ESP8266 固件用 `DEVICE_ID` 拼 topic，Python 通配符自动发现，格式必须一致 `bishe/99257/{ID}/status|flame|alarm`。

## 调试指南

### Fix #1+#2 — 连接去重

**验证启动**：Flask 日志中同一秒不应出现两个 `/video_feed` 请求。

**主动测试**：浏览器打开页面后按 F5 刷新，Flask 日志应显示旧连接和新连接各一行，无堆积。

**压力测试**：页面跑 10 分钟，期间操作仪表盘按钮、改阈值。`/dashboard` `/control` 均有响应 → 连接池未耗尽 → 修复生效。

**如果仍卡死**：在 `detector.py` 的 `generate_frames()` 中 stop_event 检查处加临时日志：
```python
if stop_event is not None and stop_event.is_set():
    print("🔌 旧连接已踢，退出生成器")
    return
```
看不到这句 → `app.py` 的 `_active_feed_stop.set()` 未执行 → 检查全局变量是否正确传递。

### Fix #3+#4 — ESP8266 非阻塞 + 重连

**LED 行为**：
| 阶段 | LED |
|------|-----|
| 上电等待 WiFi | 常亮 |
| WiFi 已连 | 慢闪（500ms） |
| MQTT 已连 | 快闪（200ms） |
| 收到 MQTT 消息 | 短暂灭一下（60ms），不阻塞 |
| WiFi 断开 | 常亮，自动重连 |

**重连测试**：关 WiFi 路由器 → LED 变常亮 → 30s 后开路由器 → ESP8266 自动重连。通过 broker `bishe/99257/{ID}/status` 主题观察 online/offline 切换。

**退避验证**：反复断连时，重连间隔应为 2s → 4s → 8s → 16s → 32s → 60s（封顶）。连上后重置回 2s。

**编译要求**：Arduino IDE 需安装 PubSubClient 库。`min()` 是 Arduino.h 内置宏，无需额外 include。

### 整体回归

合并后跑完整流程：
1. `python app.py` 无报错
2. `localhost:5000` — 视频流显示、数据刷新、按钮可点击
3. 打开 5 分钟以上，按钮依然响应
4. MQTT broker 上 `bishe/99257/alarm` 有消息发布

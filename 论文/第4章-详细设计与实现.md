# 第4章 详细设计与实现

## 4.1 开发环境与工具

　　本系统横跨三个平台的软件开发：PC端Python后端与前端、ESP32/ESP8266端Arduino固件、STM32端ARM裸机固件。各平台的开发环境与工具链配置如表4-1所示。

<p align="center"><b>表4-1 开发环境与工具链</b></p>

| 层级 | 语言/框架 | IDE/编辑器 | 核心依赖库 | 硬件平台 |
|------|----------|----------|-----------|---------|
| PC后端 | Python 3.11 | VS Code | Flask, PyTorch 2.x, Ultralytics, OpenCV, paho-mqtt | PC (RTX 3060 6GB) |
| PC前端 | HTML5/CSS3/JavaScript | VS Code | Chart.js 4.x (CDN), 原生Fetch API | 浏览器 |
| ESP32-CAM | C++ (Arduino) | Arduino IDE 2.x | ESP32 Arduino Core, esp_camera | ESP32-CAM |
| ESP8266 | C++ (Arduino) | Arduino IDE 2.x | ESP8266WiFi, PubSubClient | ESP8266 ESP-01S |
| STM32 | C (HAL) | Keil MDK 5 | STM32F1 HAL Driver, CMSIS | STM32F103ZET6 |
| YOLO训练 | Python 3.11 | VS Code | Ultralytics YOLOv8, PyTorch CUDA 12.x | RTX 3060 6GB |

　　版本控制采用Git，项目全程约80个commit，分支策略为feature分支开发、主分支审查合并的工作流。Python虚拟环境使用Miniconda管理，依赖库通过pip安装。ESP32和ESP8266固件通过USB-TTL串口模块烧录，烧录时需将GPIO0拉低进入下载模式。STM32固件使用ST-Link V2调试器完成烧录和在线调试。

## 4.2 硬件详细设计与实现

### 4.2.1 硬件引脚分配与连接

STM32的GPIO引脚围绕五类外设进行分配：LED指示、蜂鸣器报警、火焰传感器输入、舵机PWM控制、ESP8266串口通信。完整引脚映射如表4-2所示。

<p align="center"><b>表4-2 STM32 引脚映射表</b></p>

| 外设 | 引脚 | 功能说明 |
|------|------|---------|
| 黄灯LED | PE5 | 预警指示 |
| 红灯LED | PB5 | 报警指示 |
| 蜂鸣器 | PB8 | 声音报警输出 |
| 火焰传感器 | PA0 | 数字输入（低电平=火焰） |
| 舵机PWM | PA6 (TIM3_CH1) | 50Hz PWM输出 |
| ESP8266 TX→RX | PA3 (USART2_RX) | 接收WiFi模块数据 |
| ESP8266 RX←TX | PA2 (USART2_TX) | 向WiFi模块发送数据 |

ESP8266与STM32之间采用交叉串口连接，通信参数为115200bps、8数据位、无校验、1停止位。火焰传感器模块的数字输出接至微控制器GPIO输入引脚。

### 4.2.2 执行器控制实现

执行器控制系统包含三个输出维度：灯光视觉指示、蜂鸣器听觉警报和舵机物理闸门。三级输出定义如表4-3所示。

<p align="center"><b>表4-3 三级报警输出定义</b></p>

| 等级 | 触发条件 | 黄灯 | 红灯 | 蜂鸣器 | 舵机闸门 |
|:---:|---------|:---:|:---:|:---:|:---:|
| LV0 正常 | 人数 ≤ 预警阈值 | 灭 | 灭 | 灭 | 关闭 |
| LV1 预警 | 预警阈值 < 人数 ≤ 报警阈值 | 闪烁 | 灭 | 灭 | 关闭 |
| LV2 报警 | 人数 > 报警阈值 或 火灾触发 | 常亮 | 常亮 | 鸣响 | 打开 |

舵机闸门采用非阻塞控制方式，避免在闸门动作期间阻塞传感器轮询和指令接收。火焰传感器报警在微控制器固件中具有最高优先级，当火焰触发时无论当前MQTT指令为何均输出最高报警等级，确保本地火灾应急响应不因通信链路状态而受影响。

## 4.3 检测引擎详细实现

　　检测引擎是系统最核心的模块，负责多通道并行检测、双视频源管理、YOLO推理调度以及报警防抖和联动决策触发。

### 4.3.1 YOLOv8n模型结构与推理流程

YOLOv8的网络结构由三个核心部分组成[1]：Backbone采用CSPDarknet架构，通过跨阶段部分连接（CSP）策略减少计算量并改善梯度流动，nano版本的深度和宽度缩放因子分别为0.33和0.25。Neck采用FPN（特征金字塔网络）+PAN（路径聚合网络）组合结构，FPN自上而下传递深层语义特征，PAN自下而上传递浅层位置信息，两者结合使网络能同时获取丰富的语义和精确的位置信息。Head采用解耦检测头（Decoupled Head），将分类和定位任务分配到独立分支处理，减少任务间特征冲突。YOLOv8摒弃了传统的Anchor-based设计，采用Anchor-free检测方式直接预测目标中心点偏移量和边界框宽高。系统使用COCO预训练权重直接推理，仅统计person类别的检测框并过滤低置信度结果，以此作为有效行人计数。

### 4.3.2 多线程检测流水线

每个监测通道分配一个独立的检测线程，线程入口以固定频率上限循环执行。循环体依次完成以下步骤：检查通道是否活跃（处于暂停状态的通道进入休眠等待）、检测视频源配置是否发生变更并在变更时完成新旧源的切换、从当前视频源读取一帧图像、将图像缩放到模型输入尺寸后提交YOLO推理、根据推理结果统计画面中的行人数量、执行报警防抖判定、在原始帧上绘制检测标注框、将检测结果写入共享状态存储区、按固定周期写入检测记录数据库、按固定周期触发联动决策。

检测线程内部有两个关键的时间间隔参数：一是检测周期上限，与摄像头最高帧率匹配以避免无效的空帧读取。二是联动决策触发间隔，在响应实时性与锁竞争、消息量之间折中，过短会增加状态锁的竞争和MQTT消息量，过长则影响决策响应的及时性。

### 4.3.3 MJPEG流解析与非标准格式兼容

ESP32-CAM使用HTTP multipart/x-mixed-replace协议推送MJPEG视频流。开发中发现OpenCV的FFmpeg后端无法正确解析该摄像头的MJPEG格式——FFmpeg期望标准的MJPEG容器封装，而ESP32-CAM输出的是一种非标准的裸JPEG帧流，缺少容器级别的帧索引信息，导致cv2.VideoCapture直接打开URL时持续报错或返回空帧。因此系统放弃通用视频采集方案，改为手动HTTP流解析：与常见的每次请求获取单帧图像的方式不同，持久连接方案建立一次HTTP连接后持续从响应流中读取字节数据，在字节流中搜索JPEG图像的起始标记和结束标记来定位帧边界，提取出完整的一帧JPEG数据后解码为图像矩阵。连接以视频源URL和线程标识的组合作为键进行缓存，实现每线程独立的连接管理。解析过程中设置字节缓冲区的上限阈值，超出时截断保留尾部数据，防止因网络抖动导致内存无限增长。

### 4.3.4 双视频源统一接入

为使检测主循环不感知底层视频源的具体类型，设计了一个统一的帧读取接口：根据视频源描述对象的类型判断当前配置是MJPEG网络流还是MP4本地文件，分别调用对应的读取逻辑。对于MP4文件，使用OpenCV的视频捕获接口逐帧读取。当读到文件末尾时，不采用"释放后重新打开"的方案（这会导致解码器重初始化，引入数百毫秒的恢复延迟），而是通过帧指针回退到文件开头的seek方式实现循环播放。若seek失败（如文件在运行中被删除），则回退到释放后重新打开的保底逻辑。

### 4.3.5 GPU资源调度实现

三个检测线程共享一块GPU，若同时提交推理请求会导致CUDA计算资源排队等待和帧率波动。解决方案是在各线程启动时错开首次推理时机：将检测周期均匀切分为三个相位窗口，A通道在0ms启动，B通道延迟约16.7ms（1/3周期），C通道延迟约33.3ms（2/3周期）。各线程的推理请求在时间轴上均匀散布，GPU利用率趋于平稳，三通道帧率保持均衡，无明显相互干扰。这一设计使得单GPU即可支撑三路并行推理，无需多卡部署。

### 4.3.6 数据库表结构与实现

系统采用SQLite进行本地持久化，数据库包含检测记录表和报警事件表两张核心表。ER图如图4-X所示。

检测记录表（detection_records）按固定周期写入各通道的快照数据，供前端折线图查询。主要字段包括：记录ID（自增主键）、通道标识（A/B/C）、检测时间戳、当前人数、报警等级、视频源类型。查询时按通道和时间范围过滤，返回最近N条记录用于前端渲染。

报警事件表（alarm_events）以生命周期模式跟踪每次报警的完整过程。主要字段包括：事件ID（自增主键）、通道标识、报警等级（1预警/2报警）、触发时间、解除时间（未解除时为NULL）、峰值人数、峰值时间、是否火灾触发。报警触发时插入新记录并将解除时间置空，报警解除时回填解除时间和持续时长。单次报警的完整生命周期通过触发时间到解除时间的闭区间定义，支持事后统计各通道的报警频次和时长分布。

## 4.4 报警与决策详细实现

### 4.4.1 三级报警判定与对称防抖实现

三级报警等级判定。每个检测周期完成后，将当前人数与为该通道配置的预警阈值和报警阈值进行比较，确定目标报警等级：人数不超过预警阈值时目标等级为LV0（正常）；人数介于预警与报警阈值之间时目标等级为LV1（黄色预警）；人数超过报警阈值时目标等级为LV2（红色报警）。

对称防抖算法实现。为避免检测结果的偶然波动导致报警状态在阈值附近反复跳变，系统实现了对称防抖机制。上升方向（LV0→LV1、LV1→LV2、LV0→LV2）和下降方向（LV2→LV1、LV1→LV0）采用相同的确认策略：连续3帧目标等级高于或低于当前等级时，才执行等级切换。等级切换后进入3秒锁定保护期，期间忽略反向变化请求。锁定保护期结束后，重新开始计数器累计。

报警事件以生命周期模式记录：等级从低升高的时刻记录为报警触发时间，等级回降至正常的时刻记录为解除时间，期间持续追踪该通道的人数峰值及其发生时刻。一次从触发到解除的完整过程构成一条报警事件记录。

火焰优先逻辑。火焰传感器触发或手动报警激活时，对应通道的等效报警等级强制设为LV2，不受检测人数和阈值参数影响，且不经过防抖确认即时生效。

### 4.4.2 饱和度计算与四策略判定实现

联动决策模块在每个决策周期执行时，首先在互斥保护下汇总各通道状态快照，然后按以下优先级顺序进行四策略判定：

（1）若无任何活跃监测通道，判定为all_clear，提示无活跃通道无需引导。

（2）统计当前无火灾的"安全通道"列表。若安全通道数为零（所有通道均火灾），判定为emergency（紧急疏散），触发最高级别警报。

（3）计算所有安全通道的饱和度，定义为当前人数与预警阈值的比值。以饱和度而非绝对人数排序的原因是：假设通道A阈值20人、当前18人（饱和度0.9），通道B阈值5人、当前4人（饱和度0.8），按绝对人数排序会推荐B，但实际上B更接近其容量上限，应优先引导人员前往A。饱和度计算消除了不同通道阈值差异带来的容量感知偏差。

（4）若所有安全通道的报警等级均为正常且饱和度低于0.85（接近饱和判定线），判定为all_clear（全线畅通），无需进行人流引导。

（5）若存在拥挤通道但也有畅通的安全通道（报警等级正常且饱和度未达0.85），判定为guided（引导推荐），将畅通通道按饱和度从低到高排序生成推荐出口列表。

（6）若以上条件均不满足（所有安全通道均接近或超过容量上限），判定为degraded（降级通行），将所有安全通道按饱和度升序列出，建议分批通行。

### 4.4.3 每设备差异化指令生成

遍历所有当前在线且已完成通道绑定的执行设备，根据每个设备所绑定通道的报警等级和火灾状态生成差异化控制指令。指令包含三个字段：灯光等级（LV0正常/LV1预警/LV2报警）、蜂鸣器开关（开/关）、舵机开关（开/关）。灯光等级的确定规则为：若绑定通道处于火灾状态或存在手动报警信号，直接输出LV2。否则使用该通道当前的检测报警等级。蜂鸣器在灯光等级不低于LV1时开启，舵机在灯光等级达到LV2时开启。

所有MQTT消息的发送操作在锁临界区外执行，避免网络I/O阻塞检测线程。状态广播消息在内容发生变化且距上次发送超过最小间隔（0.5秒）时才发送。每设备的控制指令同理采用内容变化检测加最小发送间隔的节流策略。

## 4.5 通信协议详细实现

### 4.5.1 MQTT Topic体系与通配符订阅

系统Topic按"项目前缀/设备标识/消息类型"三级结构组织，完整Topic体系如表4-4所示。

<p align="center"><b>表4-4 MQTT Topic体系</b></p>

| 方向 | Topic格式 | 说明 |
|:---:|------|------|
| 上行 | `bishe/99257/{device_id}/status` | 设备心跳：上线/离线（遗嘱消息自动发布） |
| 上行 | `bishe/99257/{device_id}/flame` | 火焰传感器状态上报 |
| 下行 | `bishe/99257/{device_id}/command` | 控制指令：灯光等级、蜂鸣器开关、闸门控制 |
| 广播 | `bishe/99257/SIG` | 仪表盘信号：三通道人数+报警等级+推荐策略 |

通配符订阅与设备自动发现。MQTT连接成功后，客户端向Broker订阅带有单级通配符（+）的Topic模式：`bishe/99257/+/status`和`bishe/99257/+/flame`。单级通配符匹配任意设备标识符但仅限一个Topic层级，与多级通配符（#）相比，`+`限定通配范围在status和flame两个特定字段，避免意外匹配到command等其他Topic类型。无论有多少台执行设备、设备标识符是什么，只要它们按照约定的Topic层级发布消息，服务端就能自动感知到新设备的加入，无需预先配置设备清单或重启服务。

### 4.5.2 消息回调与设备状态管理

当收到来自任意设备的MQTT消息时，回调函数从消息的Topic中按层级分隔提取设备标识符和消息字段类型，然后在设备状态字典中为该设备创建或更新对应条目。设备首次出现时自动创建条目（即自动发现），之后根据消息字段类型更新对应的状态属性，状态字段更新设备的在线/离线状态，火焰字段解析火焰传感器的触发状态。消息处理完成后刷新向后兼容的聚合属性（是否有STM32设备在线、是否有火焰传感器触发），这些聚合属性被联动决策模块以引用方式读取，确保拿到的是最新值而非导入时的快照。

每设备定向发送。向指定设备下发控制指令时，消息通过"前缀/设备标识符/指令类型"的三级Topic发布，采用至少一次送达的服务质量等级确保可靠性，消息以换行符结尾便于接收端按行解析。广播接口则遍历所有在线设备，分别调用定向发送。

### 4.5.3 消息节流与分级冷却机制

串口帧边界处理。微控制器的串口接收采用按行分隔策略，以换行字符（\n）作为帧结束标记，忽略回车符，避免连续发送时的数据粘包。

MQTT消息节流。设备控制指令和状态广播消息在内容未变化时不重复发送，避免多通道同时触发报警时产生消息风暴。每设备最小发送间隔设为0.5秒。

通知冷却分级。报警通知按严重程度分级冷却：火灾消息始终即时发送，不受冷却限制。报警等级从低到高升级时，同样穿透冷却立即发送，确保管理人员不会错过事态恶化的信号。同级或降级的常规报警受冷却间隔限制：黄色预警冷却间隔为60秒，红色报警冷却间隔为30秒（紧急性越高冷却越短）。冷却计时器以通道为粒度独立维护，不同通道的报警通知互不干扰。

## 4.6 钉钉推送模块

　　推送模块实现了报警通知、恢复通知和分级冷却机制。

　　markdown消息内容设计。报警触发时构建结构化的通知消息，按四个层次组织内容。标题行：根据报警类型（火灾/红色报警/黄色预警）使用不同的警示标识，附带通道名称和当前人数。引导行：根据联动决策的策略类型生成差异化文案，紧急疏散时警告所有通道不安全并要求立即组织撤离，降级模式时提示各通道接近容量上限建议分批通行并列出推荐出口，引导模式时按饱和度排序推荐畅通出口，自由通行时不输出引导行。各通道状态概览：以警示标识加通道名称、人数和饱和度百分比的形式逐行列出，当前报警通道标注位置提示。消防提示行：对火灾通道特别标注禁止进入。

　　解除通知在报警恢复时发送，内容包含该次报警期间的峰值人数、持续时长以及当前仍在报警的其他通道提示。每通道的解除通知有独立的冷却间隔，避免频繁发送。当所有通道均恢复正常时，额外输出"所有通道恢复正常通行"的确认信息。

## 4.7 嵌入式固件实现

### 4.7.1 STM32主控固件

　　STM32固件运行在STM32F103ZET6开发板上，基于HAL库和CMSIS编写，主要实现了非阻塞舵机控制、火焰传感器防抖采集和UART指令解析三个功能模块。

　　非阻塞舵机状态机。舵机选用SG90 360°连续旋转型号，由TIM3的CH1通道（PA6引脚）输出50Hz PWM信号控制。通过改变CCR1捕获/比较寄存器的值控制脉冲宽度：约1ms脉宽对应正向旋转（开门），约1.5ms对应停止，约2ms对应反向旋转（关门）。实际使用时CCR1值在标准值基础上微调以克服机械静摩擦。闸门动作需要持续约1200ms，若采用简单延时等待将在此期间阻塞传感器轮询和UART数据接收。因此舵机控制采用基于时间状态机的非阻塞方案：收到动作指令后，记录动作起始时刻并设置舵机进入运行状态，然后即刻返回主循环。主循环每一轮迭代检查动作时长是否到达，到期后自动将舵机信号复位至停止状态。整个动作期间火焰传感器轮询和串口数据接收不受任何影响。

　　火焰传感器防抖采集。火焰传感器以固定间隔被轮询读取引脚电平。为避免瞬时电磁干扰或偶然触碰导致的误报，采用连续多次一致确认的策略：只有当连续3次读取到低电平时，才确认火警有效并立即驱动所有输出进入LV2状态，同时通过串口上传火焰触发消息。同理，解除火警也需要连续3次读到正常电平后才确认。这种防抖设计使火焰探测的单次干扰不会触发误报，同时响应延迟被控制在数百毫秒级别（约150ms），远小于1秒的实时性要求。

　　火焰报警一旦确认，其输出状态具有最高优先级，MQTT下发的常规控制指令不会覆盖火焰触发的全输出状态，确保紧急情况下本地应急响应的权威性。

　　UART指令解析。STM32通过USART2串口与ESP8266通信。接收端采用逐字节超时轮询的方式读取数据，以换行符作为帧边界标记。当接收到完整的一行数据后，按约定的键值对格式（LV:x,BUZ:x,SERVO:x）解析出灯光等级、蜂鸣器开关和舵机开关三个控制字段。解析成功后，仅在无火焰报警的前提下更新输出状态和舵机动作，保证火焰信号的最高优先级不被远程指令覆盖。

### 4.7.2 ESP8266 MQTT透传固件

　　ESP8266固件实现了"MQTT到UART双向透明桥接"功能，使STM32和PC服务端之间的通信对双方来说如同直接连接。

　　遗嘱消息机制。MQTT协议内置的遗嘱消息（Last Will Testament）机制是本系统实现设备离线自动感知的关键。ESP8266在连接Broker时预先声明一条遗嘱消息，指定遗嘱Topic为`bishe/99257/{device_id}/status`、遗嘱内容为offline、保留标志为true。当ESP8266因网络中断或断电等异常原因断线时，Broker自动将该遗嘱消息发布到指定Topic，服务端收到后即可标记设备离线。保留标志确保即使服务端在设备断线的瞬间未在线，重连后仍能收到Broker保留的最后一条遗嘱消息。设备正常上线后，主动发布online消息覆盖此前的遗嘱，完成在线状态的闭环管理。

　　双向串口透传。ESP8266与STM32之间通过USART2（PA2-TX, PA3-RX）交叉连接。下行方向（PC到STM32）：MQTT消息回调函数在收到订阅Topic的消息后，将消息载荷原样写入串口发送缓冲区，由ESP8266的UART发送至STM32。上行方向（STM32到PC）：在主循环中持续检测串口接收缓冲区是否有数据到达，有则按行读取完整的一帧数据，去除首尾空白后通过MQTT发布到对应的上行Topic。这一设计的核心价值在于透明性，STM32端像在使用有线串口直接与PC通信，PC端像在直接与MQTT设备通信，ESP8266的存在对双方完全不可见。

　　LED状态指示。利用ESP-01模组内置LED实现三级物理状态指示：常亮表示上电或正在等待WiFi连接。中速闪烁表示WiFi已连接但MQTT服务器尚未连上。快速闪烁表示MQTT通信正常。收到MQTT消息时LED短暂闪一下，作为数据收发活动的物理指示灯，方便现场调试。

### 4.7.3 ESP32-CAM固件

　　ESP32-CAM固件基于ESP32 Arduino SDK中的CameraWebServer示例修改，实现了MJPEG视频流的HTTP推送功能。

　　帧率优化。开发初期，ESP32-CAM在默认时钟配置下实测帧率只有2-3fps，远未达到实时检测的需求。排查发现默认时钟频率处于OV2640图像传感器的非标准工作区间，导致内部PLL无法稳定锁定，实际输出帧率大幅低于理论值。优化方案从三个层面展开：时钟层面，将传感器外部时钟频率提升至标准工作区间的高频段（20MHz）来稳定PLL锁定。画质层面，适当降低JPEG压缩质量参数以减少编码耗时。分辨率层面，固定输出QVGA（320×240）级别画面。同时关闭WiFi省电模式防止协议栈间歇休眠导致的传输抖动。经过这些调整后，实测帧率稳定在20fps以上，达到实时检测的基本要求。

　　MJPEG推流实现。固件初始化时配置OV2640传感器（时钟频率、分辨率、JPEG质量等参数），随后启动一个轻量级HTTP服务器，在视频流端点以multipart/x-mixed-replace的MIME类型循环输出JPEG帧，帧与帧之间用约定的边界标记分隔。客户端（PC端检测引擎）通过HTTP持久连接持续读取这个流式响应，按边界标记切分出独立的JPEG帧进行后续的AI推理。

## 4.8 前端仪表盘实现

　　前端仪表盘为单页面应用，全部代码集中在单个HTML文件中，基于原生JavaScript、Chart.js图表库和CSS3构建，不依赖前端构建工具链。

　　数据刷新策略。前端以1秒为间隔轮询后端的/dashboard合并状态接口，该接口在一次HTTP请求中返回所有通道的实时人数、报警等级、推荐引导信息、设备在线状态和折线图历史数据。为控制单次响应数据量，每通道仅返回最近300条检测记录（覆盖约5分钟的历史窗口），服务端查询时传入通道本次激活的时间戳参数过滤掉切换视频源之前的旧数据。视频画面通过三个img标签各自加载对应通道的MJPEG视频流地址，前端以亚秒级间隔刷新img标签的src属性并附加随机查询参数（如?t=时间戳），绕过浏览器的缓存策略确保画面实时更新。

　　动态折线图。折线图组件按通道区分数据系列，仅对当前活跃的通道创建数据集对象。维护一个数据集到通道的映射表，通道暂停时删除对应的数据曲线、恢复时重新创建。时间轴的处理上，将所有记录的时间戳截断到秒级实现三通道在横轴上的对齐。折线图配置为跨数据空缺连续绘制（spanGaps），避免因短暂的数据缺失造成视觉中断。服务端查询时传入通道本次激活的时间戳参数，过滤掉切换视频源之前的历史数据，确保一张图上展示的数据时间段是一致的。

　　应急双态切换。仪表盘具有正常监测和紧急应急两种视觉模式，通过页面根元素上的CSS类名（.emergency）控制全局样式切换。前端在每次状态轮询时检查应急触发条件：是否存在火灾报警或全局策略是否为emergency。满足条件时添加.emergency类触发全局视觉切换；条件解除后移除类名恢复常规界面。正常模式下，推荐引导面板使用绿色和黄色系配色。紧急模式下，顶部导航栏背景切换为红色（#dc3545）并显示"紧急疏散模式"横幅，历史趋势图区域通过CSS max-height过渡动画折叠缩小以腾出空间，推荐引导面板字体放大至1.2倍并通过order属性调整至视觉优先位置，各通道视频窗口边框根据安全状态动态着色（绿色安全、黄色预警、红色报警），使用户一眼即可感知全局态势。

　　响应式布局。通过CSS媒体查询适配移动端视口：当屏幕宽度低于680px时，三个通道的视频面板由水平并排自动切换为垂直堆叠，字体大小和间距相应缩小，确保在手机屏幕上也具备基本的可读性和可操作性。

## 4.9 YOLO模型训练

　　本系统选用YOLOv8n（nano版本）作为人员检测的基础模型，约3.2M参数量，在检测精度与推理速度之间取得了适合实时监控的平衡。

　　经实际部署前的测试评估，YOLOv8n在COCO数据集上的预训练权重对person类别的检测精度，已能满足本系统常规场景下的人员计数需求，正常光照和中等密度条件下的检测效果稳定，漏检率在可接受的范围内。基于这个评估结果，系统直接使用COCO预训练权重进行推理部署，未额外进行自定义数据集的微调训练。这是一个务实的工程判断：当前系统功能完整、检测精度达标，完整训练（数十轮）所能带来的边际精度提升不足以改变系统的功能可用性。

　　系统已完成自定义数据集的采集和标注工作，为未来在更复杂场景（如夜间低照度、高密度遮挡）下需要更高检测精度时提供了微调基础。数据集合并了公开行人检测数据集和自采集的校园场景图像，标注类别为单一的person类，按标准比例划分为训练集、验证集和测试集，标注格式采用YOLO格式（类别ID加归一化边界框坐标）。

　　为后续可能的微调准备了训练参数方案：基线模型使用COCO预训练权重，训练轮次配合早停策略防止过拟合，批次大小根据GPU显存容量设定为稳定上限，优化器选用带权重衰减的自适应矩估计（AdamW），学习率配合余弦退火调度策略，数据增强使用YOLO标准的mosaic、mixup、色彩空间扰动和几何变换等组合。这些方案已经过评估，后续如需微调可直接启动训练。
